# Core
numpy>=1.24
PyYAML>=6.0
python-docx>=1.0   # only needed if you want to re-parse job_description.docx etc. programmatically

# Embedding model (Tier 1 — best quality). Requires a one-time internet
# connection during pre-computation to download model weights; rank.py
# itself makes no network calls.
sentence-transformers>=2.2.0

# TF-IDF + LSA fallback (Tier 2 — no internet ever required). Used
# automatically if sentence-transformers/the model weights aren't
# available. Meaningfully better than naive keyword matching; see
# engine/embedder.py and the README's "Embedding tiers" section.
scikit-learn>=1.3

# sentence-transformers pulls in torch + transformers as transitive deps.

# Sandbox UI (HuggingFace Spaces / local demo)
# Using Gradio 5.x which is compatible with latest huggingface_hub
gradio>=5.0.0
